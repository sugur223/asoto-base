# プロジェクト管理機能 - 詳細設計

## 1. 機能概要

継続的な「あそと活動」をプロジェクトとして管理し、メンバー募集・タスク管理・進捗共有を行う機能。イベントと異なり、複数回の活動や中長期的な取り組みを対象とする。

### プロジェクトの種類

- **🔍 あそびプロジェクト**: 軽めの継続活動（あそとPR、哲学カフェづくり、AIで遊ぶ会など）
- **🌱 あそとプロジェクト**: 本格的な取り組み（クラファン、新百姓プロジェクト、サービス開発など）

## 2. ユーザーストーリー

### プロジェクトオーナー（佐藤さん）のケース

**背景**
- 「新百姓プロジェクト」を立ち上げたい
- 農業に興味がある人、Webで発信できる人を探している
- 週末に活動できるメンバーが欲しい

**ストーリー**
1. 「+ プロジェクト作成」ボタンをクリック
2. プロジェクト情報を入力：
   - タイトル：「新百姓プロジェクト」
   - カテゴリ：あそとプロジェクト
   - 目的：神奈川西部で"新百姓への道"を探求
   - 期間：2025-12〜2026-06（6ヶ月）
   - 活動頻度：週末月2回程度
   - 募集スキル：農業知識、写真撮影、ライティング
   - タグ：#農業 #地域 #暮らし
3. 「メンバーを募集」にチェック
4. システムが条件に合う人を自動推薦
5. 推薦された人に招待を送信
6. メンバーが集まったらプロジェクト開始
7. タスクボードで活動を管理
8. 定期的に進捗を共有 → 貢献度ポイント獲得

### プロジェクト参加者（田中さん）のケース

**背景**
- 農業と暮らしに興味がある
- 実際に体験できるプロジェクトを探している
- 写真が趣味

**ストーリー**
1. ダッシュボードに「おすすめプロジェクト」が表示
2. 「新百姓プロジェクト - マッチ度92%」を発見
3. マッチング理由を確認：
   - 興味が一致（#農業 #暮らし）
   - スキルが活かせる（写真撮影）
   - 活動時間が合う（週末）
4. プロジェクト詳細を確認
5. 「参加リクエスト」ボタンをクリック
6. オーナーが承認 → プロジェクトメンバーになる
7. タスクを担当し、活動記録を残す
8. 貢献度ポイント獲得、あそとスコア向上

## 3. 画面設計

### 3.1 プロジェクト一覧画面（/projects）

```
┌─────────────────────────────────────────────────────────┐
│  プロジェクト                                  [+ 作成]  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  📊 自分のプロジェクト  🔍 探す  💡 おすすめ            │
│                                                         │
│  フィルター: [すべて ▼] [募集中] [進行中] [完了]        │
│  カテゴリ: [すべて] [🔍 あそび] [🌱 あそと]            │
│  タグ: [すべて ▼] [#農業] [#AI] [#デザイン] [#発信]    │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 🌱 新百姓プロジェクト                        │        │
│  │                                              │        │
│  │ 神奈川西部で"新百姓への道"を探求            │        │
│  │                                              │        │
│  │ 👥 メンバー: 5/8人                           │        │
│  │ 📅 期間: 2025-12 〜 2026-06                 │        │
│  │ 🏷️ #農業 #地域 #暮らし                       │        │
│  │                                              │        │
│  │ 🔓 募集中のスキル:                           │        │
│  │    • 農業知識                                │        │
│  │    • 写真撮影 ⭐あなたに最適                │        │
│  │    • ライティング                            │        │
│  │                                              │        │
│  │ 💡 マッチ度: 92%                             │        │
│  │                                              │        │
│  │ オーナー: 👤 佐藤さん                        │        │
│  │                                              │        │
│  │ [詳細を見る] [参加リクエスト]                │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 🔍 AIで遊ぶ会                                │        │
│  │                                              │        │
│  │ AIを触ってみる遊びで更なるアイディアの      │        │
│  │ きっかけづくり                              │        │
│  │                                              │        │
│  │ 👥 メンバー: 8人（募集終了）                │        │
│  │ 📅 期間: 継続中                              │        │
│  │ 🏷️ #AI #技術 #実験                           │        │
│  │                                              │        │
│  │ 💬 月1回オンライン＋Slack                   │        │
│  │                                              │        │
│  │ オーナー: 👤 山田さん                        │        │
│  │                                              │        │
│  │ [詳細を見る]                                 │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 🌱 クラファン                                │        │
│  │                                              │        │
│  │ あそとの仲間を増やす取り組み                │        │
│  │                                              │        │
│  │ 👥 メンバー: 12人                            │        │
│  │ 📅 期間: 2025-11 〜 2026-03                 │        │
│  │ 🏷️ #資金調達 #PR #コミュニティ              │        │
│  │                                              │        │
│  │ 🔓 募集中のスキル:                           │        │
│  │    • マーケティング                          │        │
│  │    • 動画編集                                │        │
│  │                                              │        │
│  │ オーナー: 👤 鈴木さん                        │        │
│  │                                              │        │
│  │ [詳細を見る] [参加リクエスト]                │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.2 プロジェクト詳細画面（/projects/:id）

```
┌─────────────────────────────────────────────────────────┐
│  ← 戻る                                        [編集]    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  🌱 新百姓プロジェクト                                  │
│                                                         │
│  オーナー: 👤 佐藤太郎さん                              │
│  🏷️ #農業 #地域 #暮らし                                 │
│  カテゴリ: あそとプロジェクト                           │
│                                                         │
│  ─────────────────────────────────────────────        │
│                                                         │
│  📝 プロジェクト概要                                    │
│                                                         │
│  神奈川西部で"新百姓への道"を探求するプロジェクト。    │
│  農作業の体験、地域の方との交流、発信活動を通じて、    │
│  新しい暮らし方を模索します。                          │
│                                                         │
│  ─────────────────────────────────────────────        │
│                                                         │
│  📅 期間・活動頻度                                      │
│     期間: 2025-12-01 〜 2026-06-30                     │
│     活動: 週末月2回程度（土曜日 10:00-16:00）          │
│     場所: 神奈川県西部（詳細はメンバーに共有）          │
│                                                         │
│  👥 メンバー（5/8人）                                   │
│     ┌──────────────────────────────────┐              │
│     │ 👤 佐藤さん（オーナー）                 │              │
│     │ 👤 田中さん（写真担当）                 │              │
│     │ 👤 鈴木さん（ライティング）             │              │
│     │ 👤 山田さん（農業知識）                 │              │
│     │ 👤 高橋さん（SNS発信）                  │              │
│     │                                      │              │
│     │ [+ あと3人募集中]                    │              │
│     └──────────────────────────────────┘              │
│                                                         │
│  🔓 募集中のスキル                                      │
│     • 農業知識                                          │
│     • 写真撮影 ⭐あなたに最適                          │
│     • ライティング                                      │
│     • 動画編集                                          │
│                                                         │
│     [参加リクエスト]                                    │
│                                                         │
│  ─────────────────────────────────────────────        │
│                                                         │
│  📊 進捗（タブ切り替え）                                │
│                                                         │
│  [📋 タスク] [📝 活動記録] [💬 ディスカッション]         │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 📋 タスクボード                              │        │
│  │                                              │        │
│  │ To Do (3)                                    │        │
│  │  ☐ 農家さんへの訪問アポ取り                 │        │
│  │  ☐ 活動記録用カメラ準備                     │        │
│  │  ☐ SNSアカウント開設                        │        │
│  │                                              │        │
│  │ In Progress (2)                              │        │
│  │  🔄 活動スケジュール調整（田中さん）        │        │
│  │  🔄 プロジェクト紹介記事執筆（鈴木さん）    │        │
│  │                                              │        │
│  │ Done (5)                                     │        │
│  │  ✅ キックオフミーティング                  │        │
│  │  ✅ 役割分担決定                            │        │
│  │  ✅ 活動方針まとめ                          │        │
│  │  ✅ Slackワークスペース作成                 │        │
│  │  ✅ メンバー募集開始                        │        │
│  │                                              │        │
│  │ [+ タスク追加]                               │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ─────────────────────────────────────────────        │
│                                                         │
│  💬 最近のディスカッション                              │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 👤 佐藤さん  2時間前                         │        │
│  │ 来週の活動日程について相談です。             │        │
│  │ 11/23は雨予報なので、11/24に変更しませんか？│        │
│  │                                              │        │
│  │ 💬 3件の返信                                 │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 👤 田中さん  1日前                           │        │
│  │ 初回訪問の写真を共有しました！               │        │
│  │ 📷 [画像3枚]                                 │        │
│  │                                              │        │
│  │ 💬 8件の返信                                 │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  [ディスカッションを投稿]                               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.3 プロジェクト作成画面（/projects/new）

```
┌─────────────────────────────────────────────────────────┐
│  プロジェクトを作成                                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  基本情報                                               │
│  ┌─────────────────────────────────────────┐          │
│  │ プロジェクト名 *                            │          │
│  │ ┌─────────────────────────────────────┐ │          │
│  │ │ 新百姓プロジェクト                      │ │          │
│  │ └─────────────────────────────────────┘ │          │
│  │                                             │          │
│  │ カテゴリ *                                  │          │
│  │ ○ 🔍 あそびプロジェクト                    │          │
│  │ ● 🌱 あそとプロジェクト                    │          │
│  │                                             │          │
│  │ プロジェクト概要 *                          │          │
│  │ ┌─────────────────────────────────────┐ │          │
│  │ │ 神奈川西部で"新百姓への道"を探求      │ │          │
│  │ │ するプロジェクト。農作業の体験、      │ │          │
│  │ │ 地域の方との交流、発信活動を通じて... │ │          │
│  │ └─────────────────────────────────────┘ │          │
│  │ Markdown対応                                │          │
│  │                                             │          │
│  │ タグ                                        │          │
│  │ ┌─────────────────────────────────────┐ │          │
│  │ │ #農業 #地域 #暮らし                     │ │          │
│  │ └─────────────────────────────────────┘ │          │
│  │                                             │          │
│  └─────────────────────────────────────────┘          │
│                                                         │
│  期間・活動                                             │
│  ┌─────────────────────────────────────────┐          │
│  │ 開始日 *                                    │          │
│  │ ┌─────────────┐  終了日                   │          │
│  │ │ 2025-12-01  │  ┌─────────────┐       │          │
│  │ └─────────────┘  │ 2026-06-30  │       │          │
│  │                    └─────────────┘       │          │
│  │                                             │          │
│  │ 活動頻度                                    │          │
│  │ ┌─────────────────────────────────────┐ │          │
│  │ │ 週末月2回程度（土曜日 10:00-16:00） │ │          │
│  │ └─────────────────────────────────────┘ │          │
│  │                                             │          │
│  │ 活動場所                                    │          │
│  │ ○ オンライン                               │          │
│  │ ● オフライン                               │          │
│  │ ○ ハイブリッド                             │          │
│  │                                             │          │
│  │ 場所詳細                                    │          │
│  │ ┌─────────────────────────────────────┐ │          │
│  │ │ 神奈川県西部（詳細はメンバーに共有）│ │          │
│  │ └─────────────────────────────────────┘ │          │
│  │                                             │          │
│  └─────────────────────────────────────────┘          │
│                                                         │
│  メンバー募集                                           │
│  ┌─────────────────────────────────────────┐          │
│  │ ☑ メンバーを募集する                       │          │
│  │                                             │          │
│  │ 募集人数                                    │          │
│  │ ┌─────┐                                    │          │
│  │ │  8   │ 人                                │          │
│  │ └─────┘                                    │          │
│  │                                             │          │
│  │ 募集スキル                                  │          │
│  │ ┌─────────────────────────────────────┐ │          │
│  │ │ • 農業知識                              │ │          │
│  │ │ • 写真撮影                              │ │          │
│  │ │ • ライティング                          │ │          │
│  │ │ • 動画編集                              │ │          │
│  │ │ [+ 追加]                                │ │          │
│  │ └─────────────────────────────────────┘ │          │
│  │                                             │          │
│  │ ☑ システムが自動でマッチングする人を推薦  │          │
│  │                                             │          │
│  └─────────────────────────────────────────┘          │
│                                                         │
│  公開設定                                               │
│  ┌─────────────────────────────────────────┐          │
│  │ ● 公開（誰でも見られる）                   │          │
│  │ ○ メンバーのみ                             │          │
│  └─────────────────────────────────────────┘          │
│                                                         │
│  [キャンセル]                          [プロジェクト作成] │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 4. データモデル

### 4.1 Project（プロジェクト）

```python
from sqlalchemy import Column, String, Integer, DateTime, JSON, Boolean, Enum
from sqlalchemy.orm import relationship
import enum

class ProjectCategory(str, enum.Enum):
    ASOBI = "asobi"  # あそびプロジェクト
    ASOTO = "asoto"  # あそとプロジェクト

class ProjectStatus(str, enum.Enum):
    RECRUITING = "recruiting"  # 募集中
    ACTIVE = "active"          # 進行中
    COMPLETED = "completed"    # 完了
    ARCHIVED = "archived"      # アーカイブ

class Project(Base):
    __tablename__ = "projects"

    id = Column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    title = Column(String, nullable=False)
    description = Column(String)  # Markdown
    category = Column(Enum(ProjectCategory), nullable=False)
    status = Column(Enum(ProjectStatus), default=ProjectStatus.RECRUITING)

    # オーナー
    ownerId = Column(String, ForeignKey("users.id"), nullable=False)
    owner = relationship("User", back_populates="owned_projects")

    # 期間
    startDate = Column(DateTime, nullable=False)
    endDate = Column(DateTime)

    # 活動情報
    frequency = Column(String)  # "週末月2回程度"
    location = Column(String)   # "online" | "offline" | "hybrid"
    locationDetail = Column(String)

    # メンバー募集
    isRecruiting = Column(Boolean, default=False)
    maxMembers = Column(Integer)
    requiredSkills = Column(JSON)  # ["農業知識", "写真撮影"]

    # メタ情報
    tags = Column(JSON)  # ["#農業", "#地域", "#暮らし"]
    visibility = Column(String, default="public")  # "public" | "members_only"

    # リレーション
    members = relationship("ProjectMember", back_populates="project")
    tasks = relationship("ProjectTask", back_populates="project")
    discussions = relationship("ProjectDiscussion", back_populates="project")

    createdAt = Column(DateTime, default=datetime.utcnow)
    updatedAt = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

### 4.2 ProjectMember（プロジェクトメンバー）

```python
from sqlalchemy import Column, String, DateTime, Enum, ForeignKey
from sqlalchemy.orm import relationship
import enum

class MemberRole(str, enum.Enum):
    OWNER = "owner"
    MEMBER = "member"

class MemberStatus(str, enum.Enum):
    PENDING = "pending"    # 参加リクエスト中
    ACTIVE = "active"      # 参加中
    LEFT = "left"          # 退出

class ProjectMember(Base):
    __tablename__ = "project_members"

    id = Column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    projectId = Column(String, ForeignKey("projects.id"), nullable=False)
    userId = Column(String, ForeignKey("users.id"), nullable=False)

    role = Column(Enum(MemberRole), default=MemberRole.MEMBER)
    status = Column(Enum(MemberStatus), default=MemberStatus.PENDING)

    # メンバー情報
    contributionRole = Column(String)  # "写真担当", "ライティング"
    contributionPoints = Column(Integer, default=0)  # このプロジェクトへの貢献度

    # リレーション
    project = relationship("Project", back_populates="members")
    user = relationship("User")

    joinedAt = Column(DateTime)
    createdAt = Column(DateTime, default=datetime.utcnow)
    updatedAt = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

### 4.3 ProjectTask（プロジェクトタスク）

```python
from sqlalchemy import Column, String, DateTime, Enum, ForeignKey, Integer
from sqlalchemy.orm import relationship
import enum

class TaskStatus(str, enum.Enum):
    TODO = "todo"
    IN_PROGRESS = "in_progress"
    DONE = "done"

class ProjectTask(Base):
    __tablename__ = "project_tasks"

    id = Column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    projectId = Column(String, ForeignKey("projects.id"), nullable=False)

    title = Column(String, nullable=False)
    description = Column(String)
    status = Column(Enum(TaskStatus), default=TaskStatus.TODO)

    # 担当者
    assigneeId = Column(String, ForeignKey("users.id"))
    assignee = relationship("User")

    # 順序
    order = Column(Integer)

    # 期限
    dueDate = Column(DateTime)

    # リレーション
    project = relationship("Project", back_populates="tasks")

    completedAt = Column(DateTime)
    createdAt = Column(DateTime, default=datetime.utcnow)
    updatedAt = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

### 4.4 ProjectDiscussion（プロジェクトディスカッション）

```python
from sqlalchemy import Column, String, DateTime, ForeignKey, Integer
from sqlalchemy.orm import relationship

class ProjectDiscussion(Base):
    __tablename__ = "project_discussions"

    id = Column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    projectId = Column(String, ForeignKey("projects.id"), nullable=False)
    userId = Column(String, ForeignKey("users.id"), nullable=False)

    content = Column(String, nullable=False)  # Markdown

    # 返信
    parentId = Column(String, ForeignKey("project_discussions.id"))
    replies = relationship("ProjectDiscussion", remote_side=[id])

    # メタ
    attachments = Column(JSON)  # 画像などのURL

    # リレーション
    project = relationship("Project", back_populates="discussions")
    user = relationship("User")

    createdAt = Column(DateTime, default=datetime.utcnow)
    updatedAt = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

## 5. API設計

### 5.1 プロジェクト一覧取得

```python
@router.get("/projects", response_model=List[ProjectResponse])
async def get_projects(
    category: Optional[ProjectCategory] = None,
    status: Optional[ProjectStatus] = None,
    tags: Optional[str] = None,
    is_recruiting: Optional[bool] = None,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """
    プロジェクト一覧を取得

    - category: あそび/あそとでフィルタ
    - status: 募集中/進行中/完了でフィルタ
    - tags: タグでフィルタ（カンマ区切り）
    - is_recruiting: 募集中のみ表示
    """
    query = select(Project)

    if category:
        query = query.where(Project.category == category)
    if status:
        query = query.where(Project.status == status)
    if is_recruiting:
        query = query.where(Project.isRecruiting == True)
    if tags:
        tag_list = tags.split(",")
        # JSON配列内の検索（PostgreSQL）
        query = query.where(Project.tags.contains(tag_list))

    result = await db.execute(query)
    projects = result.scalars().all()

    return projects
```

### 5.2 プロジェクト作成

```python
class ProjectCreate(BaseModel):
    title: str
    description: str
    category: ProjectCategory
    startDate: datetime
    endDate: Optional[datetime]
    frequency: Optional[str]
    location: str  # "online" | "offline" | "hybrid"
    locationDetail: Optional[str]
    isRecruiting: bool
    maxMembers: Optional[int]
    requiredSkills: Optional[List[str]]
    tags: List[str]
    visibility: str = "public"

@router.post("/projects", response_model=ProjectResponse)
async def create_project(
    project_data: ProjectCreate,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """プロジェクトを作成"""

    # プロジェクト作成
    project = Project(
        **project_data.dict(),
        ownerId=current_user.id,
        status=ProjectStatus.RECRUITING if project_data.isRecruiting else ProjectStatus.ACTIVE
    )
    db.add(project)
    await db.flush()

    # オーナーをメンバーに追加
    owner_member = ProjectMember(
        projectId=project.id,
        userId=current_user.id,
        role=MemberRole.OWNER,
        status=MemberStatus.ACTIVE,
        joinedAt=datetime.utcnow()
    )
    db.add(owner_member)

    await db.commit()
    await db.refresh(project)

    # プロジェクト作成でポイント付与
    await award_points(current_user.id, 50, "project_create", db)

    return project
```

### 5.3 参加リクエスト

```python
@router.post("/projects/{project_id}/join")
async def request_to_join_project(
    project_id: str,
    message: Optional[str] = None,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """プロジェクトへの参加リクエスト"""

    # プロジェクト存在確認
    project = await db.get(Project, project_id)
    if not project:
        raise HTTPException(status_code=404, detail="Project not found")

    # 既存メンバー確認
    existing = await db.execute(
        select(ProjectMember).where(
            ProjectMember.projectId == project_id,
            ProjectMember.userId == current_user.id
        )
    )
    if existing.scalar_one_or_none():
        raise HTTPException(status_code=400, detail="Already a member or request pending")

    # 参加リクエスト作成
    member = ProjectMember(
        projectId=project_id,
        userId=current_user.id,
        status=MemberStatus.PENDING
    )
    db.add(member)
    await db.commit()

    # オーナーに通知（TODO: 通知機能実装後）

    return {"message": "Join request sent"}
```

### 5.4 タスク管理

```python
class TaskCreate(BaseModel):
    title: str
    description: Optional[str]
    assigneeId: Optional[str]
    dueDate: Optional[datetime]

@router.post("/projects/{project_id}/tasks", response_model=TaskResponse)
async def create_task(
    project_id: str,
    task_data: TaskCreate,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """タスクを作成"""

    # プロジェクトメンバー確認
    is_member = await check_project_member(project_id, current_user.id, db)
    if not is_member:
        raise HTTPException(status_code=403, detail="Not a project member")

    # タスク作成
    task = ProjectTask(
        **task_data.dict(),
        projectId=project_id
    )
    db.add(task)
    await db.commit()
    await db.refresh(task)

    return task

@router.patch("/projects/{project_id}/tasks/{task_id}/status")
async def update_task_status(
    project_id: str,
    task_id: str,
    status: TaskStatus,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """タスクのステータスを更新"""

    task = await db.get(ProjectTask, task_id)
    if not task or task.projectId != project_id:
        raise HTTPException(status_code=404, detail="Task not found")

    old_status = task.status
    task.status = status

    # 完了時の処理
    if status == TaskStatus.DONE and old_status != TaskStatus.DONE:
        task.completedAt = datetime.utcnow()

        # タスク完了者に貢献度ポイント付与
        if task.assigneeId:
            member = await db.execute(
                select(ProjectMember).where(
                    ProjectMember.projectId == project_id,
                    ProjectMember.userId == task.assigneeId
                )
            )
            member_obj = member.scalar_one_or_none()
            if member_obj:
                member_obj.contributionPoints += 10

            await award_points(task.assigneeId, 10, "task_complete", db)

    await db.commit()
    return task
```

## 6. ポイント・貢献度計算

### プロジェクト関連のポイント

| アクション | ポイント | 説明 |
|-----------|---------|------|
| プロジェクト作成 | 50pt | あそとプロジェクト |
| プロジェクト作成 | 30pt | あそびプロジェクト |
| プロジェクト参加 | 10pt | 承認された時 |
| タスク完了 | 10pt | 1タスクごと |
| ディスカッション投稿 | 3pt | 1投稿ごと |
| プロジェクト完了 | 100pt | オーナーのみ（6ヶ月以上） |
| プロジェクト完了 | 50pt | メンバー（6ヶ月以上） |

### 貢献度スコアの計算

各メンバーの `contributionPoints` は以下で加算：
- タスク完了: +10pt
- ディスカッション投稿: +3pt
- プロジェクトへの招待成功: +5pt

貢献度ランキングとして表示し、コミュニティ内での可視化に使用。

## 7. マッチング連携

プロジェクトは「人×プロジェクト」マッチングの対象となる。

### マッチングスコア計算

```python
async def calculate_user_project_match_score(
    user: User,
    project: Project,
    db: AsyncSession
) -> float:
    """
    ユーザーとプロジェクトのマッチ度を計算（0-100）
    """
    score = 0.0
    weights = {
        'skill_match': 0.35,        # スキル一致度
        'interest_match': 0.25,     # 興味一致度
        'activity_time': 0.20,      # 活動時間の合致
        'asoto_score': 0.10,        # あそとスコア
        'past_collaboration': 0.10  # 過去の協働経験
    }

    # 1. スキル一致度
    if project.requiredSkills:
        user_skills = set(user.profile.get('skills', []))
        required_skills = set(project.requiredSkills)
        if required_skills:
            skill_match = len(user_skills & required_skills) / len(required_skills)
            score += weights['skill_match'] * skill_match * 100

    # 2. 興味一致度（Jaccard係数）
    user_interests = set(user.profile.get('interests', []))
    project_tags = set(project.tags)
    if user_interests and project_tags:
        interest_jaccard = len(user_interests & project_tags) / len(user_interests | project_tags)
        score += weights['interest_match'] * interest_jaccard * 100

    # 3. 活動時間の合致
    user_available_time = user.profile.get('available_time', 0)  # 週あたり時間
    project_required_time = estimate_project_time(project.frequency)
    if user_available_time >= project_required_time:
        time_match = 1.0
    else:
        time_match = user_available_time / project_required_time if project_required_time > 0 else 0
    score += weights['activity_time'] * time_match * 100

    # 4. あそとスコア類似度
    # プロジェクトオーナーのスコアと近い人をマッチング
    owner_score = await get_asoto_scores(project.ownerId, db)
    user_score = await get_asoto_scores(user.id, db)
    score_similarity = 1 - abs(owner_score['total'] - user_score['total']) / 100
    score += weights['asoto_score'] * score_similarity * 100

    # 5. 過去の協働経験
    past_projects = await get_common_projects(user.id, project.ownerId, db)
    collaboration_score = min(len(past_projects) * 0.2, 1.0)  # 最大1.0
    score += weights['past_collaboration'] * collaboration_score * 100

    return min(score, 100.0)
```

## 8. 実装優先度

### MVP（Phase 1）
- ✅ プロジェクト作成・一覧・詳細
- ✅ メンバー募集・参加リクエスト
- ✅ 基本的なタスク管理（To Do / In Progress / Done）
- ✅ プロジェクト×ユーザーマッチング

### Phase 2
- ディスカッション機能
- タスクの期限管理・通知
- プロジェクト活動記録
- 貢献度ランキング

### Phase 3
- プロジェクトテンプレート
- ガントチャート表示
- 外部ツール連携（Slack、Notion等）
- プロジェクト完了レポート自動生成

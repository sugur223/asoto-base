# マッチング機能 - 詳細設計

## 1. 機能概要

ユーザーのプロフィール、活動履歴、あそと3要素スコアを分析し、最適な「人・機会・企業」をマッチングする機能。属人的だったマッチングをシステム化し、新しいつながりと機会を創出する。

## 2. ユーザーストーリー

### 初心者ユーザー（田中さん）のケース

**背景**
- あそとに参加して1ヶ月
- イベントには数回参加したが、深いつながりはまだ少ない
- 次に誰と話せばいいかわからない

**ストーリー**
1. ダッシュボードに「おすすめのつながり」カードが表示される
2. 「山田さん - マッチ度85%」と表示
3. マッチング理由を確認：
   - 「同じ読書会に参加している」
   - 「興味が3つ重なっている（#デザイン思考 #読書 #副業）」
   - 「お互いの強みが補完的」
4. 「メッセージを送る」ボタンをクリック
5. テンプレートメッセージが表示：「こんにちは！同じ読書会に参加していますね。デザイン思考に興味があるとのこと、ぜひお話ししたいです」
6. メッセージ送信 → つながり成立

### プロジェクト参加希望者（田中さん）のケース

**背景**
- 「新百姓プロジェクト」に興味がある
- 農業や暮らしに関心があり、写真が趣味
- 自分に合うプロジェクトを探している

**ストーリー**
1. ダッシュボードに「おすすめプロジェクト」が表示される
2. 「新百姓プロジェクト - マッチ度92%」を発見
3. マッチング理由を確認：
   - 興味が一致（#農業 #暮らし）
   - スキルが活かせる（写真撮影）
   - 活動時間が合う（週末）
4. 「詳細を見る」→「参加リクエスト」ボタンをクリック
5. オーナーが承認 → プロジェクトメンバーになる
6. 実際に活動を始める → 関係性・多動性スコア向上

## 3. 画面設計

### 3.1 マッチング一覧画面（/matching）

```
┌─────────────────────────────────────────────────────────┐
│  おすすめのつながり                           [設定 ⚙️] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  フィルター: [すべて ▼] [人] [プロジェクト] [イベント] │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 👤 人とのマッチング                          │        │
│  │                                              │        │
│  │  ┌──────────────────────────────────┐       │        │
│  │  │ 🟢 山田太郎      マッチ度: 85%    │       │        │
│  │  │                                  │       │        │
│  │  │ 📍 読書会で3回一緒に参加         │       │        │
│  │  │ 🏷️ 共通の興味: デザイン思考、読書、│       │        │
│  │  │    副業                          │       │        │
│  │  │                                  │       │        │
│  │  │ 💡 マッチング理由:                │       │        │
│  │  │  • 似た活動パターン              │       │        │
│  │  │  • お互いの強みが補完的          │       │        │
│  │  │  • 関係性スコアが近い            │       │        │
│  │  │                                  │       │        │
│  │  │ [プロフィールを見る] [メッセージ] │       │        │
│  │  └──────────────────────────────────┘       │        │
│  │                                              │        │
│  │  ┌──────────────────────────────────┐       │        │
│  │  │ 🟡 鈴木花子      マッチ度: 78%    │       │        │
│  │  │                                  │       │        │
│  │  │ 🎨 スキル: Figma, UIデザイン     │       │        │
│  │  │ 🏷️ 共通の興味: デザイン、UX      │       │        │
│  │  │                                  │       │        │
│  │  │ 💡 マッチング理由:                │       │        │
│  │  │  • デザイン分野で活動中          │       │        │
│  │  │  • あなたの多動性を補完できる    │       │        │
│  │  │                                  │       │        │
│  │  │ [プロフィールを見る] [メッセージ] │       │        │
│  │  └──────────────────────────────────┘       │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 🎯 プロジェクトとのマッチング              │        │
│  │                                              │        │
│  │  ┌──────────────────────────────────┐       │        │
│  │  │ 🌱 新百姓プロジェクト            │       │        │
│  │  │     マッチ度: 92%                │       │        │
│  │  │                                  │       │        │
│  │  │ 神奈川西部で"新百姓への道"を探求 │       │        │
│  │  │                                  │       │        │
│  │  │ 募集中のスキル:                  │       │        │
│  │  │  • 写真撮影 ⭐あなたに最適       │       │        │
│  │  │  • ライティング                  │       │        │
│  │  │  • 農業知識                      │       │        │
│  │  │                                  │       │        │
│  │  │ 👥 5/8人 | 📅 週末月2回          │       │        │
│  │  │ 🏷️ #農業 #地域 #暮らし           │       │        │
│  │  │                                  │       │        │
│  │  │ 💡 なぜおすすめ？                 │       │        │
│  │  │  • 興味が一致（農業・暮らし）    │       │        │
│  │  │  • 写真スキルが活かせる          │       │        │
│  │  │  • 週末活動で時間が合う          │       │        │
│  │  │                                  │       │        │
│  │  │ [詳細を見る] [参加リクエスト]    │       │        │
│  │  └──────────────────────────────────┘       │        │
│  │                                              │        │
│  │  ┌──────────────────────────────────┐       │        │
│  │  │ 🔍 AIで遊ぶ会                    │       │        │
│  │  │     マッチ度: 78%                │       │        │
│  │  │                                  │       │        │
│  │  │ AIを触ってみる遊びで更なる       │       │        │
│  │  │ アイディアのきっかけづくり       │       │        │
│  │  │                                  │       │        │
│  │  │ 👥 8人 | 📅 月1回オンライン      │       │        │
│  │  │ 🏷️ #AI #技術 #実験               │       │        │
│  │  │                                  │       │        │
│  │  │ 💡 なぜおすすめ？                 │       │        │
│  │  │  • AI・技術への興味              │       │        │
│  │  │  • あそびプロジェクトで気軽      │       │        │
│  │  │                                  │       │        │
│  │  │ [詳細を見る]                     │       │        │
│  │  └──────────────────────────────────┘       │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 🎉 イベントとのマッチング                  │        │
│  │                                              │        │
│  │  ┌──────────────────────────────────┐       │        │
│  │  │ デザイン思考ワークショップ       │       │        │
│  │  │     マッチ度: 88%                │       │        │
│  │  │                                  │       │        │
│  │  │ 📅 2025-11-20 19:00-21:00        │       │        │
│  │  │ 📍 オンライン                    │       │        │
│  │  │ 👥 参加予定: 12人                │       │        │
│  │  │                                  │       │        │
│  │  │ 💡 なぜおすすめ？                 │       │        │
│  │  │  • デザイン思考に興味がある      │       │        │
│  │  │  • 似た関心を持つ人が参加予定    │       │        │
│  │  │  • 関係性スコアを高められる      │       │        │
│  │  │                                  │       │        │
│  │  │ [詳細を見る] [参加する]          │       │        │
│  │  └──────────────────────────────────┘       │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.2 マッチング詳細画面（/matching/:id）

```
┌─────────────────────────────────────────────────────────┐
│  ← 戻る              山田太郎さんとのマッチング         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────┐  👤 山田太郎                         │
│  │              │     マッチ度: 85%                    │
│  │   [写真]     │     あそと歴: 6ヶ月                  │
│  │              │                                       │
│  └──────────────┘  🏷️ 興味・関心                       │
│                      デザイン思考、読書、副業、UX       │
│                                                         │
│                      💼 できること                       │
│                      Figma、UI/UXデザイン、ファシリテーション│
│                                                         │
│  ─────────────────────────────────────────────        │
│                                                         │
│  📊 マッチング分析                                      │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ マッチ度の内訳                               │        │
│  │                                              │        │
│  │  興味の一致度         ■■■■■■■■□□ 85%  │        │
│  │  スキルの補完性       ■■■■■■■□□□ 70%  │        │
│  │  活動パターンの類似性 ■■■■■■■■■□ 90%  │        │
│  │  あそとスコアの近さ   ■■■■■■■■□□ 80%  │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  🔗 つながりのきっかけ                                  │
│  • 読書会（11月）で一緒に参加                           │
│  • 読書会（10月）で一緒に参加                           │
│  • 読書会（9月）で一緒に参加                            │
│                                                         │
│  💡 このつながりで得られること                          │
│  • デザイン思考の実践的なスキル共有                     │
│  • 副業案件の情報交換                                   │
│  • 読書会での深い議論                                   │
│                                                         │
│  👥 共通のつながり（3人）                               │
│  佐藤さん、鈴木さん、高橋さん                           │
│                                                         │
│  ─────────────────────────────────────────────        │
│                                                         │
│  💬 メッセージテンプレート                              │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ こんにちは！読書会で何度か一緒になっている   │        │
│  │ [あなたの名前]です。                         │        │
│  │                                              │        │
│  │ デザイン思考に興味があるとのこと、私も最近   │        │
│  │ 学んでいます。よければ情報交換しませんか？   │        │
│  │                                              │        │
│  │ [編集する]                                   │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  [キャンセル]                    [メッセージを送る]     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.3 マッチング設定画面（/matching/settings）

```
┌─────────────────────────────────────────────────────────┐
│  マッチング設定                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 🎯 マッチングの目的                          │        │
│  │                                              │        │
│  │  何を重視しますか？（複数選択可）            │        │
│  │  ☑ 新しい人とのつながり                     │        │
│  │  ☑ スキルの向上                             │        │
│  │  ☑ プロジェクトへの参加                     │        │
│  │  ☐ 副業・仕事の機会                         │        │
│  │  ☐ メンター・メンティー関係                 │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 🏷️ 興味のある分野                            │        │
│  │                                              │        │
│  │  [デザイン] [×]  [読書] [×]  [副業] [×]    │        │
│  │  [プログラミング] [×]  [マーケティング] [×] │        │
│  │                                              │        │
│  │  [+ タグを追加]                              │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 💼 求めているスキル                          │        │
│  │                                              │        │
│  │  [Figma] [×]  [React] [×]  [Python] [×]    │        │
│  │                                              │        │
│  │  [+ スキルを追加]                            │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ ⏰ 活動時間の希望                            │        │
│  │                                              │        │
│  │  週あたりの活動時間: [5] 時間               │        │
│  │                                              │        │
│  │  活動可能な曜日:                             │        │
│  │  ☑ 平日夜  ☑ 土曜  ☑ 日曜                  │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│  ┌────────────────────────────────────────────┐        │
│  │ 🔔 通知設定                                  │        │
│  │                                              │        │
│  │  ◉ 新しいマッチングがあったら通知する       │        │
│  │  ○ 通知しない                               │        │
│  │                                              │        │
│  │  通知頻度:                                   │        │
│  │  ◉ リアルタイム                             │        │
│  │  ○ 1日1回まとめて                           │        │
│  │  ○ 週1回まとめて                            │        │
│  └────────────────────────────────────────────┘        │
│                                                         │
│                     [キャンセル]    [保存する]         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 4. データモデル

### Matching（マッチング）

```python
from sqlalchemy import Column, String, Integer, Float, DateTime, JSON, Enum
from app.database import Base
import enum

class MatchingType(str, enum.Enum):
    USER_USER = "user_user"           # 人×人
    USER_PROJECT = "user_project"     # 人×プロジェクト
    USER_EVENT = "user_event"         # 人×イベント
    USER_COMPANY = "user_company"     # 人×企業（将来）

class MatchingStatus(str, enum.Enum):
    SUGGESTED = "suggested"           # 提案中
    VIEWED = "viewed"                 # 閲覧済み
    ACCEPTED = "accepted"             # 承諾
    DECLINED = "declined"             # 辞退
    EXPIRED = "expired"               # 期限切れ

class Matching(Base):
    __tablename__ = "matchings"

    id = Column(String, primary_key=True)
    matching_type = Column(Enum(MatchingType), nullable=False)

    # マッチング対象
    from_user_id = Column(String, ForeignKey("users.id"), nullable=False)
    to_user_id = Column(String, ForeignKey("users.id"), nullable=True)
    to_project_id = Column(String, ForeignKey("projects.id"), nullable=True)
    to_event_id = Column(String, ForeignKey("events.id"), nullable=True)

    # マッチングスコア
    match_score = Column(Float, nullable=False)  # 0-100
    score_breakdown = Column(JSON)  # スコアの内訳

    # マッチング理由
    reasons = Column(JSON)  # ["共通の興味", "スキル補完", ...]
    common_interests = Column(JSON)  # 共通の興味タグ
    common_connections = Column(JSON)  # 共通のつながり

    # ステータス
    status = Column(Enum(MatchingStatus), default=MatchingStatus.SUGGESTED)
    viewed_at = Column(DateTime, nullable=True)
    responded_at = Column(DateTime, nullable=True)

    # メタデータ
    created_at = Column(DateTime, nullable=False)
    expires_at = Column(DateTime, nullable=True)  # 提案の有効期限
```

### MatchingPreference（マッチング設定）

```python
class MatchingPreference(Base):
    __tablename__ = "matching_preferences"

    id = Column(String, primary_key=True)
    user_id = Column(String, ForeignKey("users.id"), nullable=False)

    # マッチング目的
    purposes = Column(JSON)  # ["networking", "skill_up", "project", ...]

    # 興味のある分野
    interested_tags = Column(JSON)

    # 求めているスキル
    desired_skills = Column(JSON)

    # 活動時間
    available_hours_per_week = Column(Integer)
    available_days = Column(JSON)  # ["weekday_evening", "saturday", "sunday"]

    # 通知設定
    notification_enabled = Column(Boolean, default=True)
    notification_frequency = Column(String)  # "realtime", "daily", "weekly"

    # メタデータ
    created_at = Column(DateTime)
    updated_at = Column(DateTime)
```

## 5. マッチングアルゴリズム

### 5.1 人×人マッチング

```python
import numpy as np
from sklearn.metrics.pairwise import cosine_similarity
from typing import List, Dict

class UserMatchingService:
    def __init__(self, db: AsyncSession):
        self.db = db

    async def calculate_user_match_score(
        self,
        user: User,
        candidate: User
    ) -> Dict:
        """ユーザー間のマッチスコアを計算"""

        # 1. 興味の一致度（0-100）
        interest_score = self._calculate_interest_similarity(
            user.interests,
            candidate.interests
        )

        # 2. スキルの補完性（0-100）
        skill_score = self._calculate_skill_complementarity(
            user.skills,
            candidate.skills,
            user.desired_skills,
            candidate.desired_skills
        )

        # 3. 活動パターンの類似性（0-100）
        activity_score = self._calculate_activity_similarity(
            user.activity_history,
            candidate.activity_history
        )

        # 4. あそとスコアの近さ（0-100）
        asoto_score = self._calculate_asoto_score_similarity(
            user,
            candidate
        )

        # 5. 共通のつながり（0-100）
        connection_score = self._calculate_common_connections(
            user,
            candidate
        )

        # 重み付け平均
        weights = {
            'interest': 0.30,
            'skill': 0.25,
            'activity': 0.20,
            'asoto': 0.15,
            'connection': 0.10
        }

        total_score = (
            interest_score * weights['interest'] +
            skill_score * weights['skill'] +
            activity_score * weights['activity'] +
            asoto_score * weights['asoto'] +
            connection_score * weights['connection']
        )

        return {
            'total_score': round(total_score, 1),
            'breakdown': {
                'interest': round(interest_score, 1),
                'skill': round(skill_score, 1),
                'activity': round(activity_score, 1),
                'asoto': round(asoto_score, 1),
                'connection': round(connection_score, 1)
            },
            'reasons': self._generate_reasons(
                interest_score,
                skill_score,
                activity_score,
                user,
                candidate
            )
        }

    def _calculate_interest_similarity(
        self,
        interests_a: List[str],
        interests_b: List[str]
    ) -> float:
        """興味の一致度を計算（Jaccard係数）"""
        set_a = set(interests_a)
        set_b = set(interests_b)

        if not set_a or not set_b:
            return 0.0

        intersection = len(set_a & set_b)
        union = len(set_a | set_b)

        return (intersection / union) * 100

    def _calculate_skill_complementarity(
        self,
        skills_a: List[str],
        skills_b: List[str],
        desired_a: List[str],
        desired_b: List[str]
    ) -> float:
        """スキルの補完性を計算"""

        # Aが持っていてBが欲しいスキル
        a_provides_to_b = len(set(skills_a) & set(desired_b))

        # Bが持っていてAが欲しいスキル
        b_provides_to_a = len(set(skills_b) & set(desired_a))

        # 双方向の補完性
        max_possible = max(len(desired_a), len(desired_b))

        if max_possible == 0:
            return 0.0

        complementarity = (a_provides_to_b + b_provides_to_a) / (max_possible * 2)

        return complementarity * 100

    def _calculate_activity_similarity(
        self,
        history_a: List[Dict],
        history_b: List[Dict]
    ) -> float:
        """活動パターンの類似性を計算"""

        # 特徴ベクトル作成
        # [イベント参加頻度, ログ投稿頻度, プロジェクト参加数, 活動時間帯, ...]
        vector_a = self._create_activity_vector(history_a)
        vector_b = self._create_activity_vector(history_b)

        # コサイン類似度
        similarity = cosine_similarity([vector_a], [vector_b])[0][0]

        return similarity * 100

    def _calculate_asoto_score_similarity(
        self,
        user_a: User,
        user_b: User
    ) -> float:
        """あそと3要素スコアの近さを計算"""

        # 各スコアの差を計算
        diff_relationship = abs(user_a.relationship_score - user_b.relationship_score)
        diff_activity = abs(user_a.activity_score - user_b.activity_score)
        diff_sensitivity = abs(user_a.sensitivity_score - user_b.sensitivity_score)

        # 平均差（0に近いほど似ている）
        avg_diff = (diff_relationship + diff_activity + diff_sensitivity) / 3

        # 100 - 差 で類似度に変換
        similarity = 100 - avg_diff

        return max(0, similarity)

    def _calculate_common_connections(
        self,
        user_a: User,
        user_b: User
    ) -> float:
        """共通のつながりを計算"""

        # 共通の参加イベント数、共通の友人数など
        common_events = len(
            set(user_a.attended_event_ids) & set(user_b.attended_event_ids)
        )

        # スコア化（10イベント以上で100点）
        return min(100, (common_events / 10) * 100)

    def _generate_reasons(
        self,
        interest_score: float,
        skill_score: float,
        activity_score: float,
        user: User,
        candidate: User
    ) -> List[str]:
        """マッチング理由を生成"""
        reasons = []

        if interest_score > 70:
            common = set(user.interests) & set(candidate.interests)
            reasons.append(f"共通の興味: {', '.join(list(common)[:3])}")

        if skill_score > 70:
            reasons.append("お互いの強みが補完的")

        if activity_score > 80:
            reasons.append("似た活動パターン")

        # 共通イベント
        common_events = set(user.attended_event_ids) & set(candidate.attended_event_ids)
        if len(common_events) >= 3:
            reasons.append(f"同じイベントに{len(common_events)}回参加")

        return reasons

    async def find_top_matches(
        self,
        user: User,
        limit: int = 10
    ) -> List[Dict]:
        """トップNのマッチングを取得"""

        # 全ユーザーを取得（最適化: キャッシュ、インデックス活用）
        all_users = await self.db.query(User).filter(
            User.id != user.id
        ).all()

        # マッチスコア計算
        matches = []
        for candidate in all_users:
            score_data = await self.calculate_user_match_score(user, candidate)

            matches.append({
                'user': candidate,
                'score': score_data['total_score'],
                'breakdown': score_data['breakdown'],
                'reasons': score_data['reasons']
            })

        # スコア順にソート
        matches.sort(key=lambda x: x['score'], reverse=True)

        # トップN取得
        return matches[:limit]
```

### 5.2 人×プロジェクトマッチング

```python
class ProjectMatchingService:
    async def calculate_project_match_score(
        self,
        user: User,
        project: Project
    ) -> Dict:
        """プロジェクトとのマッチスコアを計算"""

        # 1. スキルマッチ度（0-100）
        skill_score = self._calculate_skill_match(
            user.skills,
            project.required_skills
        )

        # 2. 興味マッチ度（0-100）
        interest_score = self._calculate_interest_match(
            user.interests,
            project.tags
        )

        # 3. 時間的余裕（0-100）
        time_score = self._calculate_time_availability(
            user.available_hours_per_week,
            project.required_hours_per_week
        )

        # 4. あそと要素の向上可能性（0-100）
        growth_score = self._calculate_growth_potential(
            user,
            project
        )

        # 重み付け
        weights = {
            'skill': 0.40,
            'interest': 0.30,
            'time': 0.20,
            'growth': 0.10
        }

        total_score = (
            skill_score * weights['skill'] +
            interest_score * weights['interest'] +
            time_score * weights['time'] +
            growth_score * weights['growth']
        )

        return {
            'total_score': round(total_score, 1),
            'breakdown': {
                'skill': round(skill_score, 1),
                'interest': round(interest_score, 1),
                'time': round(time_score, 1),
                'growth': round(growth_score, 1)
            },
            'reasons': self._generate_project_reasons(
                skill_score,
                interest_score,
                user,
                project
            )
        }
```

## 6. APIエンドポイント

### 6.1 マッチング取得

```python
from fastapi import APIRouter, Depends
from app.services.matching import UserMatchingService

router = APIRouter()

@router.get("/api/v1/matching/users", response_model=List[UserMatchResponse])
async def get_user_matches(
    limit: int = 10,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """おすすめのユーザーマッチングを取得"""

    service = UserMatchingService(db)
    matches = await service.find_top_matches(current_user, limit)

    return matches

@router.get("/api/v1/matching/projects", response_model=List[ProjectMatchResponse])
async def get_project_matches(
    limit: int = 10,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """おすすめのプロジェクトマッチングを取得"""

    service = ProjectMatchingService(db)
    matches = await service.find_top_matches(current_user, limit)

    return matches

@router.post("/api/v1/matching/{matching_id}/accept")
async def accept_matching(
    matching_id: str,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """マッチングを承諾"""

    matching = await db.query(Matching).filter(
        Matching.id == matching_id
    ).first()

    matching.status = MatchingStatus.ACCEPTED
    matching.responded_at = datetime.utcnow()

    await db.commit()

    # 通知送信、つながり作成など

    return {"status": "accepted"}
```

## 7. 今後の拡張案

### Phase 2
- **機械学習による精度向上**: ユーザーのフィードバックから学習
- **時系列分析**: 時間帯や曜日を考慮したマッチング
- **グループマッチング**: 3人以上のチームマッチング

### Phase 3
- **企業マッチング**: 副業・転職機会のマッチング
- **イベント自動推薦**: カレンダー連携でベストタイミング提案
- **メンター/メンティーマッチング**: 経験値の差を考慮

## 8. まとめ

マッチング機能は、asotobaseの「関係性」要素を強化する中核機能です。

**重要ポイント**
- 複数の指標を組み合わせた高精度なマッチング
- マッチング理由の明示で納得感を提供
- 段階的な機械学習導入でさらなる精度向上

これにより、ユーザーは最適なつながりと機会を見つけ、あそと活動を加速できます。
